# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
coverage.html
coverage-summary.txt

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env files
.env
config.env
test_config.yaml
pr-compass
pr-compass.exe

# Personal config files (auto-ignore user configs to prevent accidental commits)
*_config.yaml
my_config.yaml
personal_config.yaml
local_config.yaml
dev_config.yaml
work_config.yaml
user_config.yaml

# TODO list (workspace-specific)
TODO.md

# Version control metadata
.jj/
